import { SignUp } from "@clerk/nextjs";

/**
 * Renders the page component for the sign-up process.
 * @returns JSX element representing the sign-up page.
 */
export default function Page() {
  return <SignUp />;
}
